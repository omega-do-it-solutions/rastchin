# سیاست امنیتی

راست‌چین با محیط‌های محلی حساسی مانند صفحات مرورگر، افزونه‌های ویرایشگر و فرایندهای برنامه‌های دسکتاپ کار می‌کند. لطفاً آسیب‌پذیری‌های مشکوک را خصوصی گزارش کنید تا نگه‌دارندگان پیش از عمومی شدن جزئیات فرصت بررسی داشته باشند.

## نسخه‌های پشتیبانی‌شده

اصلاحات امنیتی برای آخرین نسخهٔ منتشرشدهٔ هر برنامه و شاخهٔ فعلی `main` انجام می‌شوند.

| برنامه | خط نسخهٔ پشتیبانی‌شده |
| --- | --- |
| افزونهٔ مرورگر | آخرین نسخهٔ `1.1.x` |
| افزونهٔ VS Code | آخرین نسخهٔ `0.3.x` |
| یکپارچه‌ساز دسکتاپ | آخرین نسخهٔ پایدار `0.3.x` |

نسخه‌های قدیمی ممکن است اصلاح نشوند. اگر گزارش مربوط به نسخه‌ای قدیمی است، مشخص کنید که آیا در آخرین نسخه نیز بازتولید می‌شود یا نه.

## گزارش آسیب‌پذیری

با موضوع `RastChin security report` به [hi@rastchin.tools](mailto:hi@rastchin.tools) ایمیل بزنید. این مسیر خصوصی و تأییدشدهٔ گزارش است. اگر در زبانهٔ **Security** مخزن دکمهٔ **Report a vulnerability** دیده می‌شود، می‌توانید به‌جای ایمیل از فرم خصوصی GitHub استفاده کنید.

در پیام نخست، اعتبارنامه، گفت‌وگوی خصوصی، دادهٔ شخصی، کلید امضا یا نمونهٔ اثبات مخرب قرار ندهید. اگر اطلاعات بیشتری لازم باشد، نگه‌دارندگان یک مسیر خصوصی مناسب هماهنگ می‌کنند.

آسیب‌پذیری را در ایشوی عمومی، Discussion، پول‌ریکوئست، پست شبکهٔ اجتماعی یا نظر فروشگاه گزارش نکنید.

تا جایی که امن است این اطلاعات را اضافه کنید:

- برنامه و نسخهٔ درگیر، سیستم‌عامل، نسخهٔ مرورگر/ویرایشگر/میزبان و منبع نصب؛
- نوع آسیب‌پذیری و اثر واقع‌بینانهٔ آن؛
- کمترین مراحل بازتولید با دادهٔ ساختگی؛
- اینکه مشکل از مرز اعتماد، مجوز، حریم خصوصی، پشتیبان‌گیری/بازیابی، امضا، فرایند یا شبکه عبور می‌کند یا نه؛
- لاگ، stack trace یا هش فایلِ پالایش‌شده و مرتبط؛
- راهکار موقتی که آزمایش کرده‌اید؛ و
- نام ترجیحی شما و زمان‌بندی افشای موردنظرتان، در صورت تمایل.

## سپس چه می‌شود

نگه‌دارندگان دریافت گزارش را تأیید می‌کنند، آن را بازتولید و ارزیابی می‌کنند، در صورت نیاز اصلاح و انتشار را هماهنگ می‌کنند و دربارهٔ افشای مسئولانه با گزارش‌دهنده به توافق می‌رسند. مشکلات پیچیدهٔ سازگاری بالادستی ممکن است به هماهنگی با فروشندهٔ مربوط نیاز داشته باشد. تا آماده شدن اصلاح یا افشای توافق‌شده، وضعیت خصوصی می‌ماند.

پیش از انتشار جزئیات، فرصت معقولی برای بررسی در اختیار نگه‌دارندگان بگذارید. اگر تصور می‌کنید بهره‌برداری فعال یا خطر فوری برای کاربران وجود دارد، آن را به‌روشنی در گزارش بنویسید.

## مرزهای امنیتی مهم

گزارش‌ها به‌ویژه در این موارد ارزشمندند:

- دسترسی ناخواسته به میزبان مرورگر، استفاده از مجوز، انتقال محتوای صفحه یا افزایش سطح دسترسی پیام افزونه؛
- اشتباه در تشخیص هدف VS Code، نوشتن فایل دلخواه، عبور از symlink/مسیر، نشت پشتیبان، شکست تراکنش، بازیابی ناامن یا فرار اسکریپت تزریق‌شده؛
- اشتباه در تشخیص فایل اجرایی/بسته/امضای دسکتاپ، اجرای فرمان، نشت رازهای محیطی، اشکال‌زدایی در معرض شبکه، استخراج داده از renderer یا شکست پاک‌سازی؛
- به‌خطر افتادن وابستگی، CI، بسته، امضا، محضری‌سازی یا خروجی انتشار.

اشکال‌های عادی نمایش، نسخه‌های پشتیبانی‌نشدهٔ میزبان، پرسش‌های نصب و درخواست قابلیت باید از قالب‌های عمومی ایشو استفاده کنند، مگر اینکه یکی از مرزهای امنیتی بالا را درگیر کنند.

## پژوهش با حسن نیت

آزمایش با حسن نیت روی نرم‌افزار و حساب‌هایی که متعلق به خودتان است پذیرفته می‌شود. آزمایش را محلی نگه دارید یا از حساب آزمایشی اختصاصی و محتوای ساختگی استفاده کنید. به دادهٔ دیگران دسترسی نگیرید، سرویس اشخاص ثالث را مختل نکنید، پس از تأیید مشکل به آزمایش ادامه ندهید، کنترل دسترسی فروشنده را بیش از حد لازم برای نمایش اثر دور نزنید و رازها را منتشر نکنید. اگر آزمایش ممکن است بر کاربران دیگر یا زیرساخت تولید اثر بگذارد، متوقف شوید و خصوصی گزارش دهید.

راست‌چین صرفاً به‌دلیل پژوهش با حسن نیتی که این سیاست را رعایت می‌کند، اقدام حقوقی را توصیه نمی‌کند. این بیانیه مجوز آزمایش سامانه‌های اشخاص ثالث نیست؛ سیاست‌های آن‌ها و قوانین قابل‌اجرا همچنان معتبرند.

## اطلاعات انتشار

انتشارهای امنیتی بدون افشای غیرضروری دادهٔ کاربر یا جزئیات بهره‌برداری، نسخه‌های درگیر، اثر، نسخهٔ اصلاح‌شده و راهکارهای کاهش خطر را توضیح می‌دهند. در صورت تمایل گزارش‌دهنده و امکان افشای هماهنگ، نام او در قدردانی‌ها ذکر می‌شود.
