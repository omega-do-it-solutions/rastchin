# مشارکت در راست‌چین

از اینکه به خواناتر شدن متن فارسی کمک می‌کنید، بدون اینکه استفاده از محتوای فنی سخت‌تر شود، سپاسگزاریم. مشارکت می‌تواند شامل کد، آزمون، پژوهش سازگاری میزبان، متن فارسی، دسترس‌پذیری، مستندات فارسی و ابزارهای انتشار باشد. راست‌چین فقط زبان طبیعی فارسی را پشتیبانی می‌کند؛ انگلیسی فقط در جاهایی حفظ می‌شود که تعامل فنی به آن نیاز دارد.

مشارکت در این پروژه تابع [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md) است. آسیب‌پذیری‌های امنیتی باید از فرایند خصوصی [SECURITY.md](SECURITY.md) پیروی کنند و نباید در ایشوی عمومی ثبت شوند.

## پیش از شروع

1. برای جلوگیری از کار تکراری، [ایشوهای موجود](https://github.com/omega-do-it-solutions/rastchin/issues) را جست‌وجو کنید.
2. با جدول زیر برنامهٔ مالک رفتار را پیدا کنید.
3. پیش از تغییر گستردهٔ مجوز، تغییر عمومی ناسازگار، افزودن میزبان پشتیبانی‌شده، جایگزینی وابستگی، تغییر جریان داده یا بازآرایی معماری، یک ایشو باز کنید.
4. هر پول‌ریکوئست را روی یک نتیجهٔ قابل‌مشاهده برای کاربر یا یک موضوع نگه‌داری منسجم متمرکز کنید.

برای پرسش نصب که شامل تغییر کد نیست، از [SUPPORT.md](SUPPORT.md) استفاده کنید.

## راه‌اندازی محیط توسعه

نیازمندی‌های کامل فضای کاری:

- Node.js `>=24.18.1 <25`
- pnpm `11.14.0` از طریق Corepack
- Git و Bash

برخی کارهای متمرکز به Chrome 114 یا جدیدتر، VS Code و CLI آن با نام `code`، ‏PHP 8.4 با cURL یا ابزارهای بومی بسته‌بندی ویندوز، macOS یا لینوکس نیز نیاز دارند.

```bash
git clone https://github.com/omega-do-it-solutions/rastchin.git
cd rastchin
corepack enable
pnpm install --frozen-lockfile
pnpm check
pnpm build
```

برای توسعهٔ وب‌سایت، فایل محیط محلیِ نادیده‌گرفته‌شده را بدون افزودن اعتبارنامه به Git بسازید:

```bash
[ -f apps/web/.env ] || cp apps/web/.env.example apps/web/.env
pnpm dev
```

وب‌سایت در حالت عادی به راز نیاز ندارد. ارسال بازخورد و استقرار دستی از پیکربندی runtime-only مستندشده در `apps/web/docs/` استفاده می‌کنند.

## پیدا کردن برنامهٔ مالک

| بخش | پوشه | مرزهایی که باید حفظ شوند |
| --- | --- | --- |
| وب‌سایت عمومی | `apps/web` | خروجی استاتیک، رابط فارسی RTL و دسترس‌پذیر، جداسازی متن فنی LTR و مرز کوچک بازخورد PHP در همان origin |
| افزونهٔ مرورگر | `apps/browser-extension` | مجوزها/میزبان‌های صریح Manifest V3، پردازش صرفاً محلی، adapterهای محدود میزبان و برابری بستهٔ فروشگاه |
| افزونهٔ VS Code | `apps/vscode-extension` | رضایت، پیش‌بررسی هدف/نسخه، نوشتن اتمیک، پشتیبان، هش، rollback و بازیابی امن |
| یکپارچه‌ساز دسکتاپ | `apps/desktop-integrator` | بررسی هویت هدف رسمی، پایپ خصوصی CDP، وضعیت پالایش‌شده، عیب‌یابی بدون متن، اجارهٔ پاک‌سازی و رفتار fail-closed |
| قرارداد مشترک پایدار | `packages` | فقط پس از استفادهٔ اثبات‌شده در دو برنامه اضافه شود؛ این پوشه محل انباشت کد نیست |

پیش از تغییر رفتار، [docs/product.md](docs/product.md)، ‏[docs/architecture.md](docs/architecture.md) و README برنامهٔ مالک را بخوانید. فایل‌های سورس را مستقیماً میان مرز برنامه‌ها کپی یا import نکنید.

## انتظارهای پیاده‌سازی

- آزمون‌ها را از رفتار قابل‌مشاهدهٔ کاربر و مسیرهای شکست مرتبط استخراج کنید.
- ورودی را در مرزهای سامانه اعتبارسنجی و side effectهای خارجی را جدا کنید.
- پردازش محتوای صرفاً محلی را حفظ کنید و برای چیدمان یا هویت میزبان ناشناخته به‌صورت امن ادامه ندهید.
- نثر فارسی را RTL نگه دارید و کد، فرمان، مسیر، URL، نسخه، diff، نشانی ایمیل و خروجی ترمینال را LTR جدا کنید.
- فایل‌های route و entry pointهای میزبان را باریک نگه دارید؛ رفتار باید کنار feature یا service مالک آن باشد.
- برای عناصر رابط تغییریافته از Tailwind و daisyUI موجود وب‌سایت استفاده کنید. دسترسی صفحه‌کلید، HTML معنایی، فوکوس آشکار، چیدمان واکنش‌گرا، هر دو پوسته و reduced motion را حفظ کنید.
- بدون تصمیم صریح محصول، به‌روزرسانی حریم خصوصی و آزمون متمرکز، تله‌متری، گردآوری محتوا، مجوز، دسترسی میزبان یا درخواست خارجی اضافه نکنید.
- تا حد امکان از وابستگی‌های موجود استفاده کنید. وابستگی تازه باید نگه‌داری‌شده، بررسی‌شده از نظر امنیت، دارای نسخهٔ دقیق و بدون هم‌پوشانی با ابزارهای موجود باشد.
- فقط از pnpm استفاده کنید. قفل‌فایل npm، ‏Yarn یا Bun اضافه نکنید.

قالب‌بندی برنامه‌ها متفاوت است، زیرا پروژه‌های واردشده عمداً سبک تثبیت‌شدهٔ خود را حفظ کرده‌اند. با کد اطراف هماهنگ بمانید و بازنویسی مکانیکی نامرتبط انجام ندهید.

## ماتریس راستی‌آزمایی

برای هر تغییری که می‌تواند چند برنامه، پیکربندی فضای کاری، وابستگی‌ها، CI، بسته‌بندی یا نسخهٔ آمادهٔ انتشار را درگیر کند، دروازه‌های ریشه را اجرا کنید:

```bash
pnpm install --frozen-lockfile
pnpm check
pnpm build
pnpm audit:prod
pnpm audit --audit-level high
```

فرمان‌های متمرکز برنامهٔ تغییریافته را نیز اجرا کنید:

| برنامه | راستی‌آزمایی متمرکز لازم | بررسی اضافه در صورت ارتباط |
| --- | --- | --- |
| وب‌سایت | `pnpm --filter @rastchin/web check` و `pnpm --filter @rastchin/web build` | lint با PHP 8.4 و آزمون مرز درخواست برای `public/api/feedback.php`؛ بازبینی صفحات در عرض موبایل/دسکتاپ و هر دو پوسته |
| مرورگر | `pnpm --filter rastchin-browser-extension test` و `pnpm --filter rastchin-browser-extension verify` | `pnpm --filter rastchin-browser-extension package:store` برای تغییر انتشار؛ QA دستی ویژهٔ میزبان |
| VS Code | `pnpm --filter rastchin-vscode test` | `pnpm --filter rastchin-vscode package` و ماتریس آزمون دود در Extension Development Host ایزوله برای تغییر یکپارچه‌سازی/انتشار |
| دسکتاپ | `pnpm --filter rastchin-desktop-integrator test` و `pnpm --filter rastchin-desktop-integrator verify` | ساخت و اجرای چک‌لیست دود ویندوز، macOS یا لینوکس روی سیستم‌عامل هدف |

اگر بررسی‌ای روی پلتفرم شما قابل‌اجرا نیست، در پول‌ریکوئست دقیقاً بنویسید کدام مورد و چرا اجرا نشد. ترکیب پلتفرم/نسخه‌ای را که اعتبارسنجی نشده است پشتیبانی‌شده معرفی نکنید.

## تغییرات پرریسک

تغییر در این بخش‌ها به آزمون متمرکز، مستندات و توجه صریح بازبین نیاز دارد:

- مجوزهای مرورگر، match patternها، content scriptها و توضیحات فروشگاه؛
- کشف هدف VS Code، payload تزریق‌شده، تراکنش فایل، فرادادهٔ پشتیبان، هش و بازیابی؛
- اعتماد به فایل اجرایی/بسته/امضای دسکتاپ، کشف فرایند، پالایش محیط، انتقال CDP، اجارهٔ renderer و سیاست بسته‌بندی؛
- endpoint بازخورد، فیلدهای SendGrid، محدودسازی نرخ، مدیریت دادهٔ شخصی، متن حریم خصوصی یا دورهٔ نگه‌داری؛
- امضای انتشار، notarization، هویت فروشگاه، نام خروجی و گردش‌کار انتشار.

در راستی‌آزمایی دستی از محتوای ساختگی یا حساب آزمایشی استفاده کنید. متن گفت‌وگوی خصوصی، توکن دسترسی، مواد امضا، جزئیات میزبان تولید یا دادهٔ شخصی را در fixture، لاگ، تصویر، ایشو یا پول‌ریکوئست قرار ندهید.

## فایل‌های تولیدشده و رازها

پوشهٔ وابستگی‌ها، فایل `.env`، خروجی استاتیک، افزونهٔ unpacked، فایل ZIP/CRX، فایل VSIX، خروجی `dist/` در Electron، نصب‌کننده، پروفایل محلی مرورگر، لاگ، dump پایگاه داده یا کلید امضا را commit نکنید. راستی‌آزمای ریشه الگوهای رایج راز، فرادادهٔ Git تو‌در‌تو، قفل‌فایل رقیب، مسیر خصوصی و مقادیر منسوخ استقرار را رد می‌کند.

اگر خروجی تولیدشده‌ای برای بازبینی لازم است، آن را با دورهٔ نگه‌داری محدود به پول‌ریکوئست یا اجرای گردش‌کار پیوست کنید. سورس و ورودی‌های بسته‌بندی بازتولیدپذیر مرجع اصلی می‌مانند.

## پول‌ریکوئست‌ها

یک پول‌ریکوئست مفید شامل این موارد است:

- مشکل کاربر و برنامهٔ مالک؛
- کوچک‌ترین راه‌حل کامل و رفتارهایی که عمداً بدون تغییر مانده‌اند؛
- آزمون‌های اضافه یا به‌روزشده، همراه فرمان‌ها و نتایج دقیق؛
- تصویر یا ویدئو برای تغییر دیداری رابط، از جمله نمای باریک در صورت ارتباط؛
- اثر بر مجوز، حریم خصوصی، سازگاری، مهاجرت و rollback؛
- به‌روزرسانی مستندات و changelog در صورت نیاز کاربر یا نگه‌دارنده؛ و
- محدودیت‌ها یا بررسی‌های پلتفرمی باقی‌مانده.

خروجی تولیدشده و قالب‌بندی نامرتبط را از diff خارج نگه دارید. بازخورد بازبینی ممکن است تغییر محدودتر، آزمون شکست بیشتر یا شواهدی از پلتفرم میزبان درگیر بخواهد.

## دامنهٔ commit و انتشار

commitهای روشن و قابل‌بازبینی ترجیح دارند، اما نگه‌دارندگان ممکن است پول‌ریکوئست را squash کنند. برای کار توسعهٔ عادی نسخهٔ برنامه‌ها را تغییر ندهید؛ نگه‌دارندگان افزایش نسخه و انتشار را از طریق [RELEASING.md](RELEASING.md) هماهنگ می‌کنند.

## مجوز مشارکت و نشان‌های تجاری

مگر اینکه پیش از بازبینی صریحاً خلاف آن را بیان کنید، مشارکت ارسالی به این مخزن تحت [مجوز Apache 2.0](LICENSE) ارائه می‌شود. باید حق ارائهٔ اثر را داشته باشید و متن مجوز و اعلان لازم اشخاص ثالث را حفظ کنید. راست‌چین در حال حاضر قرارداد جداگانهٔ مجوز مشارکت‌کننده نمی‌خواهد.

حقوق کد، حق استفاده از نام یا لوگوی راست‌چین برای یک توزیع تغییریافته را نمی‌دهد. برای راهنمای فورک و برند، [TRADEMARK.md](TRADEMARK.md) را ببینید.
