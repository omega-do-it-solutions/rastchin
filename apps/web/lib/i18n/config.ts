/**
 * Single-locale (Persian) site constants. The site is Persian-only and served at
 * the root, so there is no locale routing, no language switcher, and no `Locale`
 * union — just the fixed document direction and language tags.
 */
export const dir = "rtl" as const;
export const htmlLang = "fa-IR";
export const ogLocale = "fa_IR";
