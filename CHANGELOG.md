# تاریخچهٔ تغییرات

این فایل تغییرات سراسریِ زیرساخت مخزن، سیاست‌ها و یکپارچه‌سازی پروژه‌ها را ثبت می‌کند. هر برنامه تاریخچهٔ تغییرات کاربرمحور و مستقل خود را در پوشهٔ خودش نگه می‌دارد.

ساختار این فایل از [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) پیروی می‌کند و هر برنامه، تا جایی که کانال توزیعش اجازه دهد، از نسخه‌بندی معنایی استفاده می‌کند.

## [منتشرنشده]

### افزوده‌شده

- افزونهٔ مرورگر، افزونهٔ VS Code و یکپارچه‌ساز دسکتاپ در یک مونوریپوی pnpm با انتشار مستقل برنامه‌ها گردآوری شدند.
- منشأ کد همهٔ کامیت‌های پاک واردشده در [docs/migration/source-snapshots.md](docs/migration/source-snapshots.md) ثبت شد.
- قرارداد کامل محصول و نقشهٔ معماری فشرده برای مجموعه‌ابزار local-first و fail-closed افزوده شد.
- دارایی‌های رسمی لوگوی راست‌چین و سیاست نشان تجاری **Omega Do IT Solutions** افزوده شد.
- زیرساخت مهندسی OmegaForge برای نگه‌داری‌های آینده با کمک عامل‌ها حفظ شد.
- سیاست‌های عمومی مشارکت، امنیت، پشتیبانی، رفتار، نشان تجاری و انتشار مستقل افزوده شد.
- بررسی CI به‌همراه گردش‌کارهای جداگانه برای بسته‌های مرورگر، VS Code، ویندوز، macOS، لینوکس و بستهٔ امضا/محضری‌شدهٔ macOS افزوده شد.
- کنترل‌های مخزن برای مجوزها، فرادادهٔ بسته، مرزهای منبع، خروجی‌های تولیدشده، رازها، مسیرهای خصوصی و قفل‌فایل‌های رقیب افزوده شد.

### تغییرکرده

- یکپارچه‌ساز دسکتاپ 0.3.2، پشتیبانی ChatGPT/Codex را به کانال پایدار منتقل کرد و جهت RTL متن فارسی تایپ یا pasteشده در composer را قابل‌اعتمادتر کرد.
- فضای کاری روی Node.js 24.18.1، ‏pnpm 11.14.0، یک قفل‌فایل، وابستگی‌های مستقیم با نسخهٔ دقیق و مجوز Apache-2.0 برای کدهای دست‌اول استاندارد شد.
- مسیرهای مربوط به مخزن‌های جدا، پیش‌فرض‌های زیرساخت خصوصی، درخت‌های خروجی انتشار و مستندات تحویل داخلی منسوخ با گردش‌کارهای مونوریپوی عمومی جایگزین شدند.
- فرادادهٔ مخزن برنامه‌ها، نیازمندی‌های اجرا، فایل‌های حقوقی، محتوای بسته‌ها، مدیریت محیط و مستندات عمومی یکدست شدند.
- نسخه‌های برنامه‌ها مستقل ماندند: مرورگر 1.1.64، ‏VS Code 0.3.12 و دسکتاپ 0.3.2.
- پیکربندی Dependabot حذف شد و به‌روزرسانی وابستگی‌ها و GitHub Actions به بررسی و اجرای دستی نگه‌دارندگان سپرده شد؛ بسته‌بندی‌های چندسکویی نیز روی هر پول‌ریکوئست اجرا نمی‌شوند.

### حذف‌شده

- برنامهٔ وب عمومی، endpoint بازخورد PHP، وابستگی‌های Next.js/React/Tailwind و همهٔ مسیرهای ساخت و استقرار وابسته به آن از مونوریپو حذف شدند.

### امنیت

- پردازش محتوای مرورگر، ویرایشگر و دسکتاپ به‌صورت محلی و بدون سرویس متمرکز راست‌چین حفظ شد.
- قواعد ایمنی رضایت، تراکنش، پشتیبان‌گیری/بازیابی، اعتماد، پایپ خصوصی، پالایش محیط، عیب‌یابی بدون متن، اجارهٔ پاک‌سازی و fail-closed حفظ شدند.
